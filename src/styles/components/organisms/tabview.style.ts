import { StyleSheet } from "react-native";

const TabviewStyle = StyleSheet.create({
  container : {
    flexDirection: 'row',
    width:'100%',
    paddingHorizontal: 50,
    justifyContent: 'space-around',
  }
});
export default TabviewStyle;
